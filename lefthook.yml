pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: npm run lint
      glob: "*.{js,ts,tsx}"
    - name: typecheck
      run: npm run typecheck
    - name: test
      run: npm test

pre-push:
  jobs:
    - name: build
      run: npm run prepare
